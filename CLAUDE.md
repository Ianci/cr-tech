# Project notes

## Branding

This project was **rebranded from "CR Tech Importaciones" to "iStock Global Importaciones"** on 2026-04-11.

- Use **iStock Global** / **iStock Global Importaciones** / **@iStock Global Importaciones** for all new UI copy, metadata, page titles, alt text, and user-facing strings.
- The repo path (`Desktop/cr-tech/stock-tracker`) and git remote are **unchanged** — do not rename folders, branches, or remotes unless explicitly asked.
- Legacy "CR TECH" strings may still exist in older components (e.g. the `CRMonogram` component, the `CR TECH` wordmark in `src/components/Footer.tsx`, the copyright line, metadata in `src/app/layout.tsx`). Update these only when working in that area — no blanket sweep unless requested.
